// A standalone build: the IntelliJ Platform SDK is large and only plugin developers need it.
pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}
rootProject.name = "specialize-idea"
